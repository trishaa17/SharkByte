'use client'; // Make sure you mark the file as a client component

import React from 'react';
import Credits from './credits'; // Make sure the path is correct based on your folder structure

export default function CreditsPage() {
  return (
    <div>
      <Credits /> {/* Render the Credits component */}
    </div>
  );
}